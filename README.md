# nullbootd
Currently an alternative to bootctl for Windows users to manage systemd-boot, so you never have to see the bootloader if you change your default from either operating system while dual-booting.

Future plans include an interactive CLI, a Linux version, A GUI, and possibly a GRUB implementation.

See [changelog](CHANGELOG.md)
